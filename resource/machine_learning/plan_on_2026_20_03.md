**1. Глубокая теория (4ч)**

- **Метрики классификации:** Изучение ROC-AUC, Precision-Recall Curve, F1-Score. Особое внимание математическому смыслу Log-Loss:
    
    $$L = -\frac{1}{N} \sum_{i=1}^{N} [y_i \log(\hat{y}_i) + (1 - y_i) \log(1 - \hat{y}_i)]$$
    
- **Оптимизация в DL:** Разбор проблемы затухающих/взрывающихся градиентов. Изучение механики RMSProp и Adam (скользящие средние первых и вторых моментов градиента).
    
- **Backpropagation:** Вывод формул обновления весов через Chain Rule для многослойного перцептрона (MLP).
    
- **Bias-Variance Tradeoff:** Математическое разложение ошибки на смещение, дисперсию и неустранимую ошибку.
    

**2. Практическая работа (7ч)**

- **Математический вывод (Obsidian):** Расписать от руки и перенести в LaTeX математический вывод Backpropagation для сети с одним скрытым слоем и функцией активации ReLU.
    
- **Решение задач на бумаге:** Самостоятельный расчет Precision, Recall и построение точек для ROC-координат на игрушечном датасете (10-15 примеров).
    
- **Структурирование знаний:** Написание конспектов по Adam и RMSProp с указанием гиперпараметров ($\beta_1$, $\beta_2$, $\epsilon$) и шага bias correction.
    

**3. Лабораторный пример (6ч)**

- **Реализация метрик с нуля:** Написание функций для вычисления ROC-AUC и Log-Loss на чистом `numpy`, без использования `sklearn`. Сравнение результатов самописных функций с библиотечными.
    
- **Имплементация оптимизатора:** Написание логики шага оптимизатора Adam для интеграции в кастомный тренировочный цикл.
    

_Пример каркаса для реализации Adam:_

Python

```
import numpy as np

def adam_step(params, grads, m, v, t, lr=0.001, beta1=0.9, beta2=0.999, eps=1e-8):
    """
    Шаг оптимизации Adam.
    params: текущие веса
    grads: градиенты на текущем шаге
    m, v: экспоненциальные скользящие средние (первый и второй моменты)
    t: номер шага
    """
    # 1. Обновить значение первого момента (m)
    # TODO: реализация
    
    # 2. Обновить значение второго момента (v)
    # TODO: реализация
    
    # 3. Выполнить коррекцию смещения (bias correction) для m и v
    # TODO: реализация
    
    # 4. Обновить параметры
    # TODO: реализация
    
    return updated_params, updated_m, updated_v
```

**4. Анализ и отчетность (3ч)**

- **Устранение пустых файлов:** Заполнение `metrics.md` ключевыми тезисами, формулами и кодом. Удаление или скрытие файлов TODO, по которым нет минимум 3 тезисов.
    
- **Перелинковка (Wiki-ссылки):** Связывание нового материала с существующим. Например, установка ссылок от статьи по Adam к `gradiet_descent.md` и `stohastic_gradient.md`.
    
- **Интеграция кода:** Добавление написанных на этапе лабораторного примера блоков кода (`numpy` реализации метрик) в соответствующие markdown-файлы конспектов.
    

Требуется ли раскрыть алгоритм расчета площади под кривой для ROC-AUC на `numpy`?