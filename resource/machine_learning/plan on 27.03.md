![[Pasted image 20260319185742.png|697]]
  Вердикт: «Скелет без мышц».
  У тебя потрясающая дисциплина в структурировании (Obsidain-архитектура, метаданные, связи), но критическая нехватка
  «плотности» знаний в базовых темах.


   * Структура (10/10): Использование статусов (DONE, in-progress), дедлайнов и связей между заметками — на уровне
     Senior-инженера. Это позволяет быстро находить пробелы.
   * Теоретическая глубина (5/10):
       * Линейные модели: Есть определения, но нет вывода формул. Нет понимания связи между функцией потерь и
         распределением ошибок (почему MSE — это Гаусс?).
       * Оптимизация (SGD): Это самый сильный конспект. Хорошо расписаны эвристики и инерция.
       * Деревья: Описан «жадный алгоритм», но пропущены асимптотики (сложность обучения/инференса) и методы борьбы с
         переобучением (pruning).
   * EDA и Метрики (2/10): Это «пустые папки». Файлы по VIF, корреляциям и категориальным признакам либо пусты, либо
     содержат только заголовки. Это фундамент Classic ML, без которого всё остальное — карго-культ.

  ---

  2. Что необходимо сделать (Recommendations)


   3. Наполнить «Пустоты» EDA: Ты не сможешь построить Classic ML модель, если не знаешь, как обрабатывать Outliers
      (выбросы), Missing Values (пропуски) и как работает Feature Selection (VIF, Boruta, Permutation Importance).
   4. Математический фундамент: Classic ML держится на оптимизации и статистике. Тебе нужно добавить в конспекты вывод
      нормального уравнения и принцип максимального правдоподобия (MLE).
   5. Закрыть «Слепые зоны» алгоритмов: У тебя полностью отсутствуют:
       * Кластеризация (K-Means, DBSCAN).
       * Снижение размерности (PCA, SVD, t-SNE).
       * Метрические методы (KNN — файл есть, но глубина слабая).
       * SVM (ядровой переход — Kernel Trick).

  ---

  6. План на следующую неделю (20 часов) — Classic ML Intensive

  Цель: Перейти от «знаю название» к «понимаю механику».